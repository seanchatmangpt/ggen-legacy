# Ultracode 25-prompt backlog progress

**Status: 25/25 complete.** Final `just ci-all`: exit 0. `git status --short`
after cleanup: 1 modified file (`tools/v26.8.1/legacy_archaeology.py`, the
GL-ARCH-003 fixes/enhancements), 9 new files (`.claude/`, `CLAUDE.md`,
`docs/v26.8.20/`, 5 tickets, `dsse_wrap.py`, `requirements.txt`,
`draft-candidates.json`). No uncommitted drift left behind — a
`coverage-matrix.csv` regeneration from item 23's read-audit (a real side
effect of running `observe`/`crown`, not an authorized write) was reverted
with `git checkout --` since it fell outside any admitted ticket's boundary.

## Summary

- **3 real bugs found and fixed** in `tools/v26.8.1/legacy_archaeology.py`
  (within GL-ARCH-003's own authored boundary): merge-commit
  `parents[0]`-only diffing, missing rename detection, compound-hash
  under-extraction in `_catalog_covered_hashes()`. Plus one enhancement
  implemented (item 15: `is_merge`/`insertions`/`deletions` triage fields).
- **1 significant security gap found**, not fixed (outside boundary):
  `appliance/bin/transparency-log.py`'s `verify()` has no external anchor —
  reproduced live truncation, un-revocation, and full-chain-rebuild attacks
  that all report `valid: true`.
- **1 significant stale-authority finding**: `authority/foundry-work-program.json`
  claims sibling-repo PRs #543/#544 are `OPEN_DRAFT`; both are actually
  `MERGED` against the real `~/ggen` repo. Flagged for the repo owner, not
  acted on (admission decisions are gated).
- **2 overclaiming corrections** to `CLAUDE.md`/tickets: the "Parity Gate"
  name/axes were this repo's own paraphrase, not the cited paper's actual
  terminology; the SLSA BLAKE3-digest framing overstated a spec restriction
  that doesn't exist (DigestSet keys are open, not a fixed enum).
- **5 tickets strengthened** with real designs, falsifiers, and pre-derived
  diffs (`GL-CONTRACT-004`/`005`/`006`/`007`) so future sessions execute
  against concrete scope instead of re-deriving it.
- **1 new working prototype**: `tools/v26.8.1/dsse_wrap.py`, real
  sign/verify/tamper-detection round-trip verified against disposable keys.
- Several smaller drift/consistency findings documented in
  `docs/v26.8.20/DECISIONS.md` (stale `verify_*.py` constants, missing
  evidence files behind `ALIVE` claims, `AGENTS.md` ticket-header staleness,
  `ci.yml`'s hardcoded workflow-count assertion, `step-two` recipe
  BUILD_BROKEN) rather than fixed, since each falls outside GL-ARCH-003's
  admitted boundary and requires its own ticket per this repo's discipline.

## Checklist

Tracker for the self-paced `/loop` working through the 25 ultracode prompts
scoped to Gall's Law checkpoints / GL-ARCH-003 / repo discipline. One prompt
executed per loop iteration, in order. Update this file's checkbox and add a
one-line outcome note each time an item completes.

- [x] 1. audit mine_structured()/draft_candidates() vs pygit2 API, adversarially verify — DONE. 5 candidate findings, 4 confirmed real (1 refuted). Fixed 3 in tools/v26.8.1/legacy_archaeology.py: merge-commit parents[0]-only diffing, missing rename detection, compound-hash under-extraction in _catalog_covered_hashes(). See tickets/GL-ARCH-003.md amendment. 4th (repo.walk re-traversal) is perf-only, left as-is. just ci-all reverified clean.
- [x] 2. cross-check CATALOG's historical_source_commit hashes vs this worktree's reachable history — DONE. ALL 79 extracted hashes across all 65 individuals are unreachable in this worktree (`git cat-file`/`rev-parse` fails on every one). This is a total, not partial, cross-repo citation pattern — the entire CATALOG documents a different repo's (`seanchatmangpt/ggen`) history, consistent with GL-ARCH-003's earlier single-hash finding, now confirmed exhaustively. Recorded in docs/v26.8.20/DECISIONS.md. Not a bug to fix (the catalog's own prose already says so per-entry) but worth flagging loudly since 100% is a stronger fact than the 2-hash spot-check implied.
- [x] 3. strengthen GL-CONTRACT-004..GL-RECEIPT-007 to GL-LSP-001/GL-PLAN-002 rigor — DONE. Self-review found GL-CONTRACT-004's Hard Law 1 required the pilot subset be "named explicitly in this file" but it wasn't — fixed by naming 5 concrete slugs (legacy_ggen_core_pipeline, legacy_process_intelligence_local_analysis, legacy_ggen_a2a_mcp_server, legacy_ggen_lsp_mcp_server, legacy_ggen_lsp_a2a_bridge) with rationale and a substitution escape hatch. GL-MANUFACTURE-005/GL-VERIFY-006/GL-RECEIPT-007 reviewed, no equivalent self-contradiction found.
- [x] 4. design minimal ContractRule + pre/postcondition schema extension for 3-5 capability pilot — DONE. Schema designed (ggen:ContractRule, precondition/postcondition string literals, dependsOnRule), written into GL-CONTRACT-004.md. Real finding: 3 of 5 named pilots have ordering_requirements="UNKNOWN" (no real prose), violating the ticket's own Hard Law 3 — escape hatch triggered, only 2 pilots (legacy_ggen_core_pipeline, legacy_process_intelligence_local_analysis) survive; 3 replacements still needed before execution, recorded in the ticket.
- [x] 5. adversarially verify AgentModernize BSG paper (arXiv:2605.17535) claims vs this repo's TTL/OWL model -- DONE. All 3 claims (BSG as trust-boundary DAG, spec-derived oracles, exact BER figures 9.4/8.1/19.4%) confirmed against real paper text. Found real divergence: paper's model is G=(V,E,Pre,Post,Inv) -- GL-CONTRACT-004's schema has no Inv (invariant) slot. Documented as a named, deliberate omission in the ticket (no real individual's prose expresses an invariant yet).
- [x] 6. audit equivalence_runner.py's four observable-surface checkers for edge cases/gaps -- DONE. Real bug: receipt_fields/event_order never call normalize_text() despite docstring claiming they do (timestamp normalization silently skipped). Also: AdapterResult.timed_out is dead data (hung adapter can pass as REFUSED), empty output passes as equivalent, zero test coverage beyond stdout/exit_code. Recorded as pre-work findings in tickets/GL-VERIFY-006.md -- not fixed directly (file is outside GL-ARCH-003's authored boundary; fix waits for GL-VERIFY-006's admission).
- [x] 7. design ParityGateReceipt Rust struct (coverage_projection.rs pattern), verify vs real fixtures -- DONE. Struct+functions designed following check_provenance_receipt's exact pattern, written into GL-VERIFY-006.md as pre-derived design. Real gap found: equivalence_runner.py's report only records manifest_path (string), never a digest -- nothing today detects manifest swap/edit between runs.
- [x] 8. research SLSA v1.0 digest-algorithm constraints, document BLAKE3-vs-sha256 open question -- DONE, real correction found. GL-RECEIPT-007 originally claimed BLAKE3 'is not a standard in-toto digest algorithm name' implying non-conformance; verified against real spec text that DigestSet keys are an OPEN JSON object (spec's own example: {"somecoolhash":"abcd"}) -- a blake3 key is spec-legal, no registration needed. Real constraint is consumer interop (spec says consumers MUST ignore unrecognized keys), not spec conformance. Ticket corrected.
- [x] 9. review every verify_*.py's hardcoded EXPECTED_* constants vs current git state -- DONE. All 5 distinct 40-char SHA constants across verify_foundry_provenance.py/verify_foundry_bootstrap.py/verify_docs.py/verify_offline_transport.py are unreachable in this worktree (git cat-file fails on every one); verify_docs.py and verify_offline_transport.py also compare directly against live HEAD (f9b283e...) which matches none of them -- stale. verify_autonomic_finish.py/verify_ggen_create_bundle.py/verify_lsp_contract.py/verify_ggen_v26_8_1_migration.py have no EXPECTED_* constants. Consistent with item 2's cross-repo finding. Documented, not fixed (outside GL-ARCH-003 boundary).
- [x] 10. audit transparency-log.py's hash-chain verify() for tamper-pass gaps -- DONE, real security gap found and reproduced. entry_hash correctly covers all fields (no unhashed-field bypass) and per-entry index/prev_hash sequencing is enforced, BUT verify() has no external anchor -- reproduced 3 live attacks (truncate tail, un-revoke by dropping the revocation entry, full chain rebuild dropping a middle entry) that all report {valid:true, error:null}. Concretely: a revoked entry can be silently un-revoked by an attacker with file write access. Not fixed (outside GL-ARCH-003 boundary) -- worth a dedicated ticket given severity.
- [x] 11. research strangler-fig prior art, draft exact schema diff for migration-manifest.schema.json routing states -- DONE. Exact diffs for schemas/migration-manifest.schema.json (disposition enum) and scripts/verify_foundry_bootstrap.py (EXPECTED_DISPOSITIONS) derived and written into GL-MANUFACTURE-005.md as ready-to-apply pre-work, not yet applied (ticket NOT_STARTED).
- [x] 12. find every standing claim in repo, verify each still holds vs current state -- DONE. authority/project-001-promotion.json cites 9 evidence files backing its ALIVE claims; 4-5 are missing on disk (docs/book/index.html, docs/book/print.html, ggen-legacy-verifier-v26.8.1.tar.gz/.receipt.json, evidence/foundry-runtime-candidate.json). The 'Foundry runtime candidate: ALIVE' claim's sole cited evidence file doesn't exist at all. 6 of 9 backing files do exist and check out. Documented; not fixed (evidence-file provisioning is outside GL-ARCH-003's boundary and may be a build-artifact-not-checked-in situation, not a defect -- flagged for repo owner confirmation).
- [x] 13. audit CLAUDE.md/AGENTS.md for internal contradictions vs actual repo structure -- DONE. CLAUDE.md's 8 canonical-authority files all exist, no contradiction there. Real gap: AGENTS.md:8-9 still only names GL-LSP-001/GL-PLAN-002 as active/concurrent tickets -- GL-ARCH-003 (executed this session, code live in legacy_archaeology.py) and the 4 drafted tickets aren't reflected. Not fixed directly (AGENTS.md is outside GL-ARCH-003's declared authored boundary -- editing it would be self-expanding scope outside ticket-gated admission); flagged as a recommendation for the repo owner or a follow-up ticket.
- [x] 14. review coverage_projection.rs end to end (correctness, dead code, test gaps) -- DONE. 5 real correctness issues found (silent standing-drop for unmatched subsystem names, resolve_root can walk into wrong repo root on nested checkouts, no --locked on internal cargo build, exact_head swallows all git errors to "UNKNOWN" indistinguishable from real unknown), 1 dead code fn (read_coverage_csv_bytes, unreferenced), zero unit tests in the file. cargo clippy --all-targets --locked -- -D warnings: clean, 0 warnings (verified with real touch+rerun to avoid stale cache). Documented, not fixed (outside GL-ARCH-003 boundary).
- [x] 15. research PyDriller/RepoDriller APIs, identify fields to strengthen draft-candidates.json -- DONE AND IMPLEMENTED (within GL-ARCH-003's own boundary, pygit2-only, no new dependency). Added is_merge/insertions/deletions to MinedCommit and draft-candidates.json output. Reverified: mine-structured runs clean, TTL untouched, just ci-all passes.
- [x] 16. audit foundry/bootstrap.yaml vs authority/foundry-work-program.json consistency -- DONE. The two files ARE internally consistent with each other (workstream ids/order/count, standing values, runtime_dependency_admitted all agree). BUT major finding: both are stale vs the real sibling repo -- checked ~/ggen via gh pr view: PR #544 and #543 (cited as OPEN_DRAFT, standing_transferred:false) are actually MERGED (2026-08-01), with further commits since. Flagged prominently in GL-MANUFACTURE-005.md as a repo-owner decision, NOT acted on (flipping the admission flag is itself gated).
- [x] 17. find every TODO/FIXME/UNASSIGNED across ontology/tickets/schemas, prioritized effort estimate -- DONE. Zero TODO/FIXME anywhere in the three trees. 132 UNASSIGNED hits, almost all (128) in legacy-capabilities.ttl: confirmed exactly 65/65 equivalenceVerifier + 65/65 negativeFalsifier + 65/65 exactHeadReceipt UNASSIGNED (100%, no exceptions). Prioritized: equivalenceVerifier (large, real blocker) > negativeFalsifier (large, sequenced after) > exactHeadReceipt (small-medium, scriptable once unblocked). GL-CONTRACT-004's 5-pilot is the correctly-scoped next step.
- [x] 18. adversarially re-verify CLAUDE.md's Gall's Law prior-art table citations against real sources -- DONE. 4/5 confirmed accurate (GraphRepo, strangler-fig 4-phase, SLSA v1.0 builder.id, Feathers characterization testing). Real correction: the Parity Gate paper (arXiv:2607.28271) exists but does NOT use the term "Parity Gate" or the three named axes (paragraphs-hit sets/ordered stub logs/terminal-state diff) -- its actual mechanism is called "Locksmith Loop"/deterministic parity checks. CLAUDE.md corrected to label this as this repo's own synthesis, not a term drawn from the paper.
- [x] 19. design+implement minimal DSSE envelope wrapper around build-standing-portfolio.py signing, verify round-trip -- DONE AND IMPLEMENTED. tools/v26.8.1/dsse_wrap.py (new, additive, no production key material touched). Real selftest verified: sign+verify round-trip succeeded, tamper detection correctly failed with real openssl error. Correction: actual signing step lives in run-reference-e2e.sh, not build-standing-portfolio.py as assumed -- corrected in GL-RECEIPT-007.md. Wiring into the real signing script remains that ticket's NOT_STARTED scope.
- [x] 20. audit 65 CATALOG individuals for dispositions that should be UNKNOWN per file's own criteria -- DONE. 3 real violations found: legacy_sync_audit_flag, legacy_sync_dry_run_value_flag, legacy_ggen_toml_dual_schema all assert a definite disposition while historical_source_commit is literally "UNKNOWN" -- inconsistent with the file's own stated criterion (later notes cite resolving evidence but historical_source_commit itself was never updated). 5 weaker secondary candidates also flagged (self-aware hedged notes, less severe). Not fixed (touching CATALOG data needs the same evidence discipline this audit didn't perform) -- documented in DECISIONS.md for a future session.
- [x] 21. review draft-candidates.json for duplicates of existing CATALOG entries under different slugs -- DONE. Zero duplicates found. Cross-referenced all 22 drafts' 1,360 unique paths against all 65 catalogued individuals' legacy_source_path fields (path-overlap, not just commit-hash). Drafts cover disjoint subsystems entirely (CI/migration-scaffolding paths) vs catalog (crates/ and justfile paths) -- genuinely separate observer pass, no re-flagging.
- [x] 22. research Feathers characterization/golden-master testing, design golden-trace corpus format for equivalence_runner.py -- DONE. Full JSON schema (ggen.legacy-equivalence.golden-trace-corpus.v1) + 3-entry worked example designed, reusing existing runner vocabulary; integration point identified (one case-source branch in run_case, all 10 existing SURFACE_CHECKERS reused unmodified). Recorded in GL-VERIFY-006.md.
- [x] 23. audit every just recipe / CI workflow file for drift vs what actually passes now -- DONE. All fmt/check/clippy recipes (root + v26) pass clean. 2 real, currently-reproducible problems found: ci.yml:34-35 hardcodes workflow-count==1 but repo now has 2 workflow files (would fail next CI run); tools/v26.8.1/justfile's step-two recipe fails for real (exit 2, BUILD_BROKEN) on an unreachable git object, consistent with items 2/9's cross-repo finding. Documented in DECISIONS.md, not fixed (outside GL-ARCH-003 boundary).
- [x] 24. find all wasm4pm references repo-wide, verify none became a direct native dependency -- DONE, clean verdict. Checked all 4 Cargo.toml files in repo (root, tools/ggen-verifier-cli-verify, tools/dsrust-disposition-proposer, tools/v26.8.1) -- zero direct [dependencies] entries for wasm4pm/wasm4pm-compat anywhere. Both enter only transitively via the admitted lsp-max git dependency and its own dependents (affidavit, lsp-max-protocol). Boundary rule currently satisfied.
- [x] 25. adversarially review this session's GL-ARCH-003 implementation end to end vs real command output -- DONE. All 4 claims PASS on independent re-run: commit-count invariant holds (420->422 live drift, both sides agree), TTL byte-identical (sha256sum match), just ci-all exit 0, all 3 amendment fixes confirmed present at cited line ranges. One flag: ticket's acceptance block hardcoded a stale '420' instead of the live invariant -- fixed.
