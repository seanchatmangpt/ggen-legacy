# Chatman Ecosystem Corpus

The authored source is [`authority/chatman-ecosystem/ecosystem.json`](../../authority/chatman-ecosystem/ecosystem.json).

The readable architecture view is [`authority/chatman-ecosystem/architecture.md`](../../authority/chatman-ecosystem/architecture.md).

SHACL admission law is stored under [`ontology/chatman-ecosystem/`](../../ontology/chatman-ecosystem/). The verifier manufactures the RDF projection from the canonical JSON authority before SHACL evaluation, so the graph cannot drift from its source.

The corpus distinguishes:

- ecosystem capabilities;
- independently governed repository realizations;
- explicit morphisms and contracts;
- authority-transfer boundaries;
- evidence and standing;
- the bounded `2026-07-31` same-day repository cohort.

It does not promote source identity, runtime execution, release, certification, or aggregate ecosystem standing beyond the evidence carried by the independent verifier report.
