version = "26.8.1"
baseline_ref = "main@8387ed060c05096d67895c38144ac7d7499a9a52"
required_document_count = 100
sunset_blocked_by_unknown = true

[validation]
minimum_documents = 80
require_unique_paths = true
require_standing = true
require_legacy_disposition = true
require_verifier_assignment = true
require_authority_mapping = true
require_implementation_mapping = true
require_zero_unknown_for_sunset = true

[standing]
allowed = ["UNKNOWN", "PARTIAL_ALIVE", "ALIVE", "BLOCKED", "BUILD_BROKEN", "UNSUPPORTED"]

[disposition]
allowed = ["PRESERVED", "SUBSUMED", "REPLACED", "ARCHIVED", "REFUSED", "UNKNOWN"]

[baseline]
repository = "seanchatmangpt/ggen"
workspace_version = "26.8.2"
workspace_packages = 18
pipeline = ["Resolve", "Enrich", "Extract", "Render", "Write", "Receipt"]

[release]
claim = "v26.8.1 is not admitted until all crown verifiers pass against one exact aggregate source head"

[sunset]
claim = "ggen-legacy sunset is refused while any mapped capability has UNKNOWN standing or UNKNOWN disposition"
