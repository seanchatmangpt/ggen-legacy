# Fortune 5 synthetic receiver case: order-core modernization

This case is a synthetic composite. It demonstrates a Fortune 5-scale decision surface without claiming a customer deployment.

## Receiver role

`ggen-create` contributes workstreams A–D: exact observation, bounded alignment, a receiving contract, and a deterministic model. `ggen-legacy` owns E–K: template authority, real `ggen` manufacture, independent acceptance, replay, release, sunset, and portfolio standing.

The receiver therefore refuses a producer bundle that claims behavioral equivalence, release, or sunset. A valid producer receipt is necessary but cannot self-certify receiver-owned consequences.

## Executed fixture

The fixture binds producer head `a0a9133095e5114b693d62a85fc287fe76425c2e`, producer version `0.4.0`, and repository `seanchatmangpt/ggen-create`. It contains a four-file synthetic order-routing predecessor plus the exact deterministic receiving bundle in a path-safe, digest-bound `tar+gzip` capsule.

The independent receiver verifies:

- accepted schema versions and exact producer repository, version, and commit;
- regular-file-only bundle topology;
- exact output set, SHA-256 digest, and POSIX mode for every generated byte;
- recomputed bundle digest, receipt digest, and manifest subject digest;
- A–D/E–K ownership separation;
- all receiver-owned claims remain `UNKNOWN`;
- exact predecessor replay, including content, size, mode, deletion, special-file, symlink, and unadmitted-file drift;
- tampered outputs, changed source, attempted self-certification, and forged producer identity become `BUILD_BROKEN`.

The producer-forgery test rewrites producer identity in the manifest, receiving contract, and receipt, then recomputes output hashes, modes, bundle digest, and receipt digest. The receiver still refuses it because integrity cannot manufacture authority.

## Enterprise consequence

The case closes intake admission only. It creates a lawful starting point from which pricing, allocation, store continuity, returns, and audit workstreams can proceed independently. No generated artifact grants ambient actuation, and predecessor sunset remains `UNKNOWN` until separate production evidence and customer authority exist.
