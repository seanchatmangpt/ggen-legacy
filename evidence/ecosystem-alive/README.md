# Ecosystem ALIVE Evidence Boundary

This directory records authored observations about the ggen-driven ecosystem promotion program. It does not own generated standing or controller receipts.

## Executed evidence

- The exact-source reconstitution matrix completed all 26 admitted Git objects successfully.
- The ggen manufacturing rail built the pinned kernel with `nightly-2026-06-22`, validated the semantic graph, manufactured the doctor projections twice, verified both ggen receipts, and observed byte-identical replay.
- The Workstream C diagnostic is idempotent: an already `ADMITTED` checkpoint produces a read-only no-op and performs no repository mutation.
- The receipt-ownership controller preserved the historical initialization receipt, selected the sole current owner of the capability catalog, normalized the active receipt view, and passed exact Rust receipt replay.

## Promotion boundary

These bounded rails are `ALIVE` from observed execution. Aggregate ecosystem standing remains `UNKNOWN` until all product repositories close native validation, negative-control, ggen-receipt, clean-replay, and independent-promotion obligations and the A–K foundry reaches terminal admission.

A queued, pending, skipped, or approval-blocked GitHub workflow is not execution evidence.
