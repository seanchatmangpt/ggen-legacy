# Generated by ggen. Do not edit directly — re-run `ggen sync` to update.
#
# Wire into your Phoenix router (lib/*_web/router.ex):
#
#   scope "/api" do
#     forward "/agents", {{ router_module | default(value="MyAppWeb.A2ARouter") }}
#   end

defmodule {{ router_module | default(value="MyAppWeb.A2ARouter") }} do
  @moduledoc """
  A2A agent router — auto-generated by ggen.

  Endpoints per agent (relative to mount point):
  {% for agent in agents %}
  - POST  /{{ agent.path | default(value=agent.agentName) }}             — {{ agent.description | default(value=agent.agentName) }}
  - GET   /{{ agent.path | default(value=agent.agentName) }}/.well-known/agent-card.json
  {% endfor %}
  """

  use Plug.Router

  plug(:match)
  plug(:dispatch)

{% for agent in agents %}
  forward "/{{ agent.path | default(value=agent.agentName) }}",
    to: A2A.Plug,
    agent: {{ agent.appModule | default(value="MyApp") }}.Agents.{{ agent.agentName | pascal }},
    base_url: System.get_env("BASE_URL", "http://localhost:4000/api/agents")

{% endfor %}
end
