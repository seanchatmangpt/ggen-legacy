[project]
name = "ggen-legacy-lsp"
version = "26.8.1"
description = "Dependency-free executable reference LSP for ggen Turtle, Tera, and TOML surfaces"
requires-python = ">=3.11"
dependencies = []

[project.scripts]
ggen-lsp = "ggen_lsp.server:main"

[tool.unittest]
start-directory = "tests"
pattern = "test_lsp_*.py"
