# NASA Dark Mode Independent Replay

This receiver verifies the exact `seanchatmangpt/ggen` source head and independently classifies the manufactured NASA Dark Mode Roku, WASM, WebGL2, and preserved deck.gl-plan bodies.

It preserves the boundary:

```text
NASA/EONET/GIBS authority
→ exact source Git objects
→ deterministic capsule manufacture
→ locally executed WebGL2 browser projection + Roku SceneGraph package
→ intent/consequence/refusal receipts
→ replay
```

The receiver does not grant physical-Roku standing without observed device execution. Locally executed browser evidence cannot substitute for a Roku device receipt, and no CI result is required for this receiver crown.

## Local object and runtime replay

```bash
python3 packs/nasa-dark-mode-replay/verify_source.py \
  --source-repo /path/to/ggen \
  --contract packs/nasa-dark-mode-replay/receiving-contract.json \
  --output evidence/nasa-dark-mode/independent-verifier.json
```

Use `--require-git-head` only against an exact Git checkout.

## No-CI receiver crown

The receiver also requires the source's locally executed no-CI receipt, compiled WASM body, 2,072-profile JS/WASM equivalence, headed Chromium WebGL2 evidence, deterministic Roku ZIP identity, strict BrightScript-derived simulation, and mutation-control deaths. It preserves deck.gl package transport, Rust toolchain, and physical Roku execution as typed blocked boundaries rather than inferring them.
