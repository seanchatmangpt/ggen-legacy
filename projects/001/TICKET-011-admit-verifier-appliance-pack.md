# TICKET-011 — Admit the ggen-legacy Verifier Appliance pack

## Authority

`AGENTS.md`, `RELEASE_CONTROL.md`, `product/PRD.md`, `architecture/ARD.md`, and `ontology/assurance-program.ttl`.

## Exact ggen coordinate

`seanchatmangpt/ggen@0f39227c102e0ac7519f0f27561356227a518653`.

## Scope

Admit one local ggen pack that manufactures an offline customer-controlled Repository Standing Portfolio compiler, independent verifier, hidden-challenge interface, digest/provenance binder, signature verifier, transparency chain, deterministic replay executor, testing-authority checks, legal-recourse checks, and separate Release/Sunset decision engine.

## Allowed paths

`ggen.toml`, `ontology/`, `packs/ggen-legacy-assurance-pack/`, `appliance/`, `tests/fixtures/`, `evidence/appliance/`, Project 001 documentation, and exact-head workflow changes.

## Forbidden paths

Unrelated application services, direct production actuation, customer source upload, hosted standing assignment, fabricated external certification, or real predecessor deletion.

## Positive witness

`bash appliance/bin/run-reference-e2e.sh` crosses real filesystem, subprocess, OpenSSL cryptography, append-only log, verifier-report, receipt, and replay boundaries.

## Negative falsifiers

Evidence tamper and implementer/verifier identity collapse must be refused while preserving the customer source input.

## Replay

Two clean portfolio builds over the same admitted inputs must produce the same tree digest.

## Expected state

The bounded reference assurance fixture may reach `ALIVE`. Product implementation remains `PARTIAL_ALIVE` or `UNKNOWN` until exact-head ggen manufacture and GitHub Actions complete. External production and real Sunset Admission remain `UNKNOWN`.
